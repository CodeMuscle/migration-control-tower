/**
 * @migrationtower/worker-sync — BullMQ worker: ongoing source<->destination sync jobs (idempotent).
 *
 * Module 1 bootstrap placeholder. Real implementation arrives in the
 * corresponding module (see /CLAUDE.md → Suggested implementation order).
 */
export const PACKAGE_NAME = "@migrationtower/worker-sync" as const;
