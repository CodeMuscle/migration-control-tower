/**
 * @migrationtower/control-plane-web — Internal control-plane web app (placeholder; framework added in its module).
 *
 * Module 1 bootstrap placeholder. Real implementation arrives in the
 * corresponding module (see /CLAUDE.md → Suggested implementation order).
 */
export const PACKAGE_NAME = "@migrationtower/control-plane-web" as const;
