{
  "name": "@migrationtower/customer-portal",
  "version": "0.0.0",
  "private": true,
  "description": "Customer-facing migration portal (placeholder; framework added in its module).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "eslint \"src/**/*.ts\" --max-warnings=0",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"@migrationtower/customer-portal: no tests yet\"",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "devDependencies": {
    "@migrationtower/tsconfig": "workspace:*",
    "@types/node": "20",
    "typescript": "^5.4.5"
  }
}
