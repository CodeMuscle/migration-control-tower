/**
 * @migrationtower/customer-portal — Customer-facing migration portal (placeholder; framework added in its module).
 *
 * Module 1 bootstrap placeholder. Real implementation arrives in the
 * corresponding module (see /CLAUDE.md → Suggested implementation order).
 */
export const PACKAGE_NAME = "@migrationtower/customer-portal" as const;
