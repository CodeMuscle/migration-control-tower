{
  "name": "@migrationtower/sdk-core",
  "version": "0.0.0",
  "private": true,
  "description": "Framework-agnostic TypeScript client for the Migration Tower API.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "eslint \"src/**/*.ts\" --max-warnings=0",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"@migrationtower/sdk-core: no tests yet\"",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "devDependencies": {
    "@migrationtower/tsconfig": "workspace:*",
    "typescript": "^5.4.5"
  }
}
