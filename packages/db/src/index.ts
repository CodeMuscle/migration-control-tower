/**
 * @migrationtower/db — Database client, schema, and migrations (every table is tenant-scoped).
 *
 * Module 1 bootstrap placeholder. Real implementation arrives in the
 * corresponding module (see /CLAUDE.md → Suggested implementation order).
 */
export const PACKAGE_NAME = "@migrationtower/db" as const;
